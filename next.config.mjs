// Importing type definitions for better IntelliSense and type checking
/** @type {import('next').NextConfig} */
const nextConfig = {
    
    images: {
      domains: [
        'localhost'
      ],
    },
  
  
  };
  
  export default nextConfig;
  